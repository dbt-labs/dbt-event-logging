name: 'logging'
version: '1.0'

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"

on-run-start:
  - "{{ logging.log_target_create_audit_schema() }}"
  - "{{ logging.log_target_create_audit_log_table() }}"
  - "{{ logging.log_run_target_start_event() }}"

on-run-end:
  - "{{ logging.log_run_target_end_event() }}"
